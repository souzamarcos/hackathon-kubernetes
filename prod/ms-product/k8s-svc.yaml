apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  labels:
    app: ms-product
  name: svc-ms-product
  annotations:
    konghq.com/strip-path: 'true'
spec:
  parentRefs:
    - name: kong
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /product
    backendRefs:
    - name: ms-product
      kind: Service
      port: 8080